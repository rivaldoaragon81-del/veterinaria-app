import { ResaltarCitaDirective } from './resaltar-cita.directive';

describe('ResaltarCitaDirective', () => {
  it('should create an instance', () => {
    const directive = new ResaltarCitaDirective();
    expect(directive).toBeTruthy();
  });
});
